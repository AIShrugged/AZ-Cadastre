node_modules
build

# Prisma-generated client (regenerated from schema.prisma via `prisma generate`)
generated

.env
.env.local

.agents
.claude
skills-lock.json

# nx cache
.nx

# coverage reports (unit and integration are measured separately)
coverage
coverage-integration

# tsc --build fingerprints; they belong with build/, and nx caches them together
*.tsbuildinfo
