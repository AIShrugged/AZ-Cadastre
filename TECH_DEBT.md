# Known weaknesses

Not a wish list. Each entry says **what is not done**, **how it will fire** and
**what to do when it does**. Check here before fixing a bug: half of what looks
like one is a compromise someone already decided to take.

---

## 1. No outbox — a domain event can be lost after its write

**Not done.** `VerificationPackageRepositoryAdapter.save` writes the aggregate in
a transaction, then publishes its events and calls `commit()`. Publication is
outside the transaction, which is the right order — a subscriber must find the
row already written — but it means a process that dies between the commit and the
publish has a package in the database whose event nobody ever saw.

**How it fires.** A package sits at `Pending` forever: the write landed,
`PackageSubmitted` never reached the handler that starts the run, and nothing
retries. The inspector sees an upload that never began.

**What to do.** Re-saving the aggregate republishes its uncommitted events, so a
manual re-save clears one instance. The fix is an outbox table written inside the
same transaction and drained by a poller. It is not worth it while the events
stay in-process (ADR-0001); it becomes required the day they cross a broker.

## 2. One contracts package for a repository that expects more contexts

**Not done.** `libs/api-contracts` holds the published language of every context.
`reference/contracts.md` asks for one package per context, so that a consumer
depends on the language it uses and not on all of them.

**How it fires.** The second context arrives, puts its DTOs in the same package,
and now the browser bundle carries both — plus, once integration events exist,
there is no `./events` entry to keep the server-side ones out of it.

**What to do.** Split at that moment, not before: with one context the two
shapes are the same tree. Split into `libs/contracts/<ctx>` with `.` and
`./events` entries, and add the lint rule that forbids `apps/web` the `./events`
entry. Doing it now would be structure with nothing in it.

## 3. No API set and no browser set

**Not done.** Two of the four sets in `reference/testing.md` exist: unit and
integration. There is no test that speaks to the process over HTTP, and none
that drives a browser.

**How it fires.** A change to a gateway controller, to the exception filter's
status mapping, or to the shape a DTO serialises into passes lint, types and both
test sets, and breaks the client. Nothing between the context's inbound port and
the browser is covered by anything.

**What to do.** The API set first — `apps/server/test/**/*.e2e.spec.ts` over a
typed REST client in `libs/testing/`, with the same containers the integration
set already brings up. The browser set only after there is a screen worth
guarding.

## 4. The generated Prisma client is not in git

**Not done.** `reference/architecture.md` keeps the generated database client in
version control, because an image build has no live database. Here it is
gitignored and regenerated by a `postinstall` hook.

**How it fires.** An image built without running `postinstall` — or a CI cache
that restores `node_modules` while skipping lifecycle scripts — produces a
package with no client and a type error in every file that imports it. The moment
it bites harder is when typed SQL (`prisma/sql`) is introduced: those need a live
database at generate time, which a build machine does not have.

**What to do.** For now, make sure any Dockerfile runs `prisma generate`
explicitly rather than relying on the install hook. Commit the client before
introducing typed SQL.

## 5. Read models are counted in SQL, and the aggregate is not asked

**Not done, on purpose — recorded so it is not mistaken for a mistake.**
`PackageQueriesAdapter` computes the summary counts with its own queries rather
than loading the aggregate. Read and write therefore share a database but not a
path, and a rule that changes what "classified" means has two places to change.

**How it fires.** A field's meaning shifts in the domain, the write side follows,
and the summary keeps counting the old way. Nothing type-checks the two against
each other.

**What to do.** The integration set now covers both sides against the same
database, so a divergence shows up as a failing count. Keep it that way: any new
count on the read side gets a spec that submits through the command bus and
asserts through the query bus, never one that inserts rows directly.
